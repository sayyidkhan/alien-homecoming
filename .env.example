# Public Lovable/Vite configuration. This URL is safe to expose to browsers.
VITE_WORLD_API_URL=https://alien-homecoming-universe.<your-cloudflare-subdomain>.workers.dev

# Tigris credentials never belong in this frontend file. For local Worker
# development, copy worker/.dev.vars.example to worker/.dev.vars instead.

# Local development only
LOVABLE_API_KEY=